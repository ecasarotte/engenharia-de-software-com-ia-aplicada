# Playwright-only setup

This project configures Playwright to test the app at https://erickwendel.github.io/vanilla-js-web-app-example/ using Chromium only.

## Commands

- `npm install`
- `npx playwright install --with-deps chromium`
- `npm test`
